-- ============================================================================
-- Clicker Kingdom - 친구 대전 시작 시점 동기화 패치
-- Run this in your Supabase SQL Editor
-- ============================================================================

-- 배틀이 실제로 시작된 시각을 서버 타임스탬프로 기록.
-- 호스트/게스트 모두 이 값을 폴링해서 같은 시각에 타이머를 시작하도록 함.
-- (기존에는 게스트가 join 즉시 자기 타이머를 시작하고, 호스트는 최대 2초 뒤
--  폴링으로 감지 후 시작해서 서로 다른 시점에 배틀이 끝나는 문제가 있었음)
ALTER TABLE rooms ADD COLUMN IF NOT EXISTS battle_start_at timestamptz;
